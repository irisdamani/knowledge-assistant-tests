import type { QueryResponseDto } from '../services/knowledge/dto/query.dto';

/**
 * Alias used by assertion helpers so they remain independent of the
 * service layer's DTO naming.
 */
export type QueryResponse = QueryResponseDto;
